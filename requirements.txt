# SmartTravel Python API – production dependencies
# Install: pip install -r requirements.txt

# Web API
flask>=3.0.0
gunicorn>=21.0.0

# Data & climate
pandas>=2.0.0
numpy>=1.24.0
rasterio>=1.3.0

# Climatemaps: climate rasters (temperature, precipitation, etc.)
# Requires data/raw/ from a clone of the repo (set CLIMATEMAPS_DATA_DIR or use SmartTravel/climatemaps)
git+https://github.com/KarlisKV/climatemaps.git

# Exchange rates (currency endpoints)
yfinance>=0.2.0

# Exchange-rate fallback & IP geolocation (nationality detection)
requests>=2.28.0

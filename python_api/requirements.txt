# SmartTravel Python API – production dependencies
# Install: pip install -r requirements.txt

# Web API
flask>=3.0.0
gunicorn>=21.0.0

# Data & climate
pandas>=2.0.0
numpy>=1.24.0
rasterio>=1.3.0
matplotlib>=3.8.0

# Climatemaps: climate rasters (temperature, precipitation, etc.)
# (climatemaps imports matplotlib at load time but does not declare it in setup.py)
# Requires data/raw/ from a clone of the repo (set CLIMATEMAPS_DATA_DIR or use SmartTravel/climatemaps)
git+https://github.com/KarlisKV/climatemaps.git

# Exchange rates (currency endpoints)
yfinance>=0.2.0

# Exchange-rate fallback & IP geolocation (nationality detection)
requests>=2.28.0

matplotlib>=3.8
cartopy~=0.24.1
numpy
scipy~=1.15.3
fastapi~=0.115.12
geojsoncontour==0.5.1

rasterio>=1.3.0
pydantic>=2.5.3
uvicorn>=0.37.0
citipy>=0.0.6
pycountry>=24.6.1
geopy>=2.4.1

# SmartTravel Python API – production dependencies
# Install: pip install -r requirements.txt

# SmartTravel API — Python dependencies
# Install: pip install -r requirements.txt

# ── web framework ─────────────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0      # includes uvloop + httptools for max performance
gunicorn>=22.0.0               # production process manager

# ── async HTTP client ─────────────────────────────────────────────────────────
httpx>=0.27.0                  # replaces requests; async + connection pooling

# ── response cache ────────────────────────────────────────────────────────────
cachetools>=5.3.0

# ── data / climate ────────────────────────────────────────────────────────────
pandas>=2.0.0                  # for flags.parquet only
pyarrow>=15.0.0                # parquet backend
rasterio>=1.3.0                # GeoTIFF reads in get_climate_data.py
numpy>=1.26.0

# ── install climatemaps from GitHub (see get_climate_data.py) ─────────────────
# pip install git+https://github.com/KarlisKV/climatemaps.git



# Exchange rates (currency endpoints)
yfinance>=0.2.0

# Exchange-rate fallback & IP geolocation (nationality detection)
requests>=2.28.0

matplotlib>=3.8
cartopy~=0.24.1
numpy
scipy~=1.15.3
fastapi~=0.115.12
geojsoncontour==0.5.1

rasterio>=1.3.0
pydantic>=2.5.3
uvicorn>=0.37.0
citipy>=0.0.6
pycountry>=24.6.1
geopy>=2.4.1
